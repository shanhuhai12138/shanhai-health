# 项目纠察方案 — MindCare AI 心理健康管理平台

> 生成时间：2026-07-17  
> 分支：codex/0714  
> 技术栈：Spring Boot 3.5.14 / Java 21 / Vue 3 + Element Plus / MyBatis / MySQL / Redis

---

## 一、项目架构总览

```
health (根项目)
├── health-admin          ← Spring Boot 入口 (端口 8090)
├── health-framework      ← 安全/数据源/配置/拦截器
├── health-system         ← 系统管理 (用户/角色/菜单/字典/部门)
├── health-quartz         ← 定时任务 (Quartz)
├── health-generator      ← 代码生成器 (Velocity)
├── health-common         ← 通用工具/注解/常量/异常
├── health-reservation    ← 核心业务 (预约/测评/情绪/咨询/报告/通知)
├── health-ai             ← AI 对话 (LangChain4j + DashScope)
└── health-Vue3           ← 前端 (端口 80, 代理到 8090)

[废弃目录]
├── health-assessment/    ← 已删除，功能迁移至 health-reservation
├── health-counseling/    ← 已删除，功能迁移至 health-reservation
├── health-mood/          ← 已删除，功能迁移至 health-reservation
└── health-oss/           ← 空目录，未集成到 Maven 构建
```

**模块统计：**
- 数据库表：52 张（系统 20 + Quartz 11 + 业务 21）
- Java 实体类：38 个
- Mapper 接口：38 个
- Service 接口：37 个
- REST Controller：23 个
- 前端 API 模块：24 个
- 前端视图页面：60+ 个

---

## 二、问题清单

### 🔴 P0 — 严重（编译/启动错误）

#### 问题 1：SecurityConfig.java 被删除，导致 Spring Security 配置缺失

**影响范围：** 整个应用无法启动

**现象：**
- `health-framework/src/main/java/.../config/SecurityConfig.java` 已被删除（git status 显示 D）
- 但 `SysLoginService.java` 仍注入 `AuthenticationManager` Bean
- 没有任何其他配置类提供 `AuthenticationManager` 或 `SecurityFilterChain`
- `BCryptPasswordEncoder` Bean 也不存在
- `SysLoginService` 第 76 行调用 `authenticationManager.authenticate()` 将抛出 `NoSuchBeanDefinitionException`

**证据：**
```
// health-framework/.../SysLoginService.java:43
@Resource
private AuthenticationManager authenticationManager;

// health-framework/.../SysLoginService.java:76
authentication = authenticationManager.authenticate(authenticationToken);
```

**根因：** 重构时将 SecurityConfig.java 删除，但没有在其它配置类中重新定义这些 Bean。

**修复方案：** 恢复 SecurityConfig.java，或将其中的 Bean 定义迁移到现有配置类（如 ApplicationConfig.java）。

---

### 🟠 P1 — 高（功能不完整）

#### 问题 2：TOrdersettingController 存在两个副本，冲突且行为不一致

**影响范围：** health-admin 和 health-reservation 各有一个 TOrdersettingController

**现象：**
- 两个 Controller 都映射到 `/reservation/ordersetting`
- health-admin 版本包含更多功能：`getOrderSettingByMonth`、`editNumberByOrderDate`、`downloadTemplate`、`upload`（内联 POI 实现）
- health-reservation 版本是标准 CRUD：`list`、`getInfo`、`add`、`edit`、`remove`、`export`、`downloadTemplate`、`getOrderSettingByMonth`、`upload`
- health-admin 版本额外注入了 `TOrdersettingMapper`，而 health-reservation 版本只注入 Service
- Spring Boot 启动时会报 `ConflictingBeanDefinitionException`（同包同名 Bean 冲突）

**证据：**
```
health-admin/src/main/java/.../TOrdersettingController.java  (219 行)
health-reservation/src/main/java/.../TOrdersettingController.java (164 行)
```

**修复方案：** 删除 health-admin 下的副本，保留 health-reservation 版本（更规范），或将 health-admin 的版本合并到 health-reservation 后从 health-admin 移除。

---

#### 问题 3：咨询师排班管理 — 后端有 API 但前端完全没有

**影响范围：** 咨询师排班 CRUD 功能不可用

**后端已有：**
- `CounselorScheduleController` — 8 个端点（list/get/add/edit/remove/counselor/{id}/available/{date}/export）
- 对应 Service、Mapper、Entity、VO 齐全
- 菜单权限已配置（menu.sql 中 202 心理咨询 → 2022 预约管理）

**前端缺失：**
- ❌ 无 `src/api/reservation/schedule.js` API 模块
- ❌ 无对应的 Vue 页面（搜索 "schedule" 无结果）

**修复方案：**
1. 创建 `health-Vue3/src/api/reservation/schedule.js`
2. 创建 `health-Vue3/src/views/reservation/schedule/index.vue` 页面
3. 确认 menu.sql 中有对应的权限标识 `reservation:schedule:list` 等

---

#### 问题 4：用户端预约 API — 后端有 API 但前端无消费者

**影响范围：** 用户端预约/套餐浏览/订单管理无法在前端使用

**后端已有：**
- `UserReservationController` — 6 个端点（匿名访问）
  - GET `/reservation/user/setmeals` — 可预约套餐列表
  - GET `/reservation/user/setmeal/{id}/details` — 套餐详情
  - GET `/reservation/user/available-dates` — 可预约日期
  - POST `/reservation/user/order` — 创建预约订单
  - PUT `/reservation/user/cancel/{reportNo}` — 取消预约
- 对应 Service `IUserReservationService` 实现完整

**前端缺失：**
- ❌ 无 `src/api/reservation/userReservation.js` API 模块
- ❌ 无用户端预约页面（当前前端只有管理后台，没有面向患者的 C 端页面）

**修复方案：**
1. 创建 `health-Vue3/src/api/reservation/userReservation.js`
2. 创建面向患者的预约页面（可选：在 health-Vue3 中新增路由 `/patient/` 或使用独立前端项目）
3. 或在管理后台增加"患者预约"管理视图

---

#### 问题 5：t_report_image 表无对应实体类和 Mapper

**影响范围：** 体检报告的图像数据无法通过 ORM 操作

**数据库有表：** `t_report_image`（在 clean.sql 中定义）
- 列：id, report_id, checkitem_id, image_path, image_desc, create_time

**Java 端缺失：**
- ❌ 无 `ReportImage` 实体类
- ❌ 无 `TReportImageMapper` 接口
- ❌ 无对应的 Mapper XML

**修复方案：**
1. 创建 `TReportImage` 实体类（继承 BaseEntity）
2. 创建 `TReportImageMapper` 接口及 XML
3. 在 `TReportController` 或新建 `ReportImageController` 中提供图像上传/查询 API
4. 前端 `reservation/report.js` 增加图像相关 API

---

#### 问题 6：公告已读用户列表 — 前端有页面但后端返回类型不明确

**影响范围：** 公告管理 → 已读用户 页面可能报错

**现象：**
- `SysNoticeController.readUsersList()` 返回 `List<?>`（泛型擦除）
- `ISysNoticeReadService.selectReadUsersByNoticeId()` 返回类型需确认
- 前端 `ReadUsers.vue` 期望分页数据

**修复方案：** 确认返回类型是否为分页包装对象，必要时改为具体类型。

---

### 🟡 P2 — 中（功能缺失/冗余）

#### 问题 7：health-oss 目录是空壳，未集成到 Maven 构建

**影响范围：** 对象存储功能不可用

**现象：**
- `health-oss/` 目录存在，有 `target/classes/` 编译产物（AliyunOSSOperator.class 等）
- 但根 pom.xml 中未声明 `health-oss` 模块
- 无 `src/` 源码目录
- 无 `pom.xml` 文件

**修复方案：**
- 选项 A：删除 `health-oss/` 目录（如果不需要 OSS 功能）
- 选项 B：补充 `pom.xml` 和 `src/` 源码，在根 pom.xml 中添加模块声明，在 health-framework 中添加依赖

---

#### 问题 8：前端 API 覆盖不全 — 大量后端端点无前端调用

**缺失的前端 API 模块：**

| 后端端点 | 所属 Controller | 前端状态 |
|----------|----------------|----------|
| `/reservation/schedule/*` | CounselorScheduleController | ❌ 无 API 模块 |
| `/reservation/user/*` | UserReservationController | ❌ 无 API 模块 |
| `/notification/types` | NotificationController | ❌ 无 API 函数 |
| `/tool/gen/column/{tableId}` | GenController | ❌ 无 API 函数 |
| `/tool/gen/download/{tableName}` | GenController | ❌ 无 API 函数 |
| `/tool/gen/batchGenCode` | GenController | ❌ 无 API 函数 |
| `/system/post/optionselect` | SysPostController | ❌ 无 API 函数 |
| `/system/role/optionselect` | SysRoleController | ❌ 无 API 函数 |
| `/tool/build/*` | 表单构建器 | ❌ 无 API 模块（但有页面） |

**缺失的导出 API 函数（约 20 个）：**
几乎所有后端 Controller 都有 `POST /export` 端点，但前端 API 模块中很少配套导出函数。

**修复方案：** 按需补充前端 API 函数，特别是导出功能和下拉选项接口。

---

#### 问题 9：订单设置标准 CRUD 前端覆盖不足

**现象：**
- 后端 `TOrdersettingController` 有标准 CRUD（list/getInfo/add/edit/remove/export）
- 前端 `ordersetting.js` 只有月查询、人数编辑、新增、上传模板等特定操作
- 缺少标准的列表查询、编辑、删除 API 调用

**修复方案：** 在 `ordersetting.js` 中补充 `listOrderSetting`、`editOrderSetting`、`delOrderSetting`、`exportOrderSetting` 等函数。

---

#### 问题 10：体检报告导入模板下载端点前端未调用

**现象：**
- 后端 `TReportController.importTemplate()` 提供模板下载
- 前端 `report.js` 有 `importReport` 但没有 `downloadTemplate` 函数

**修复方案：** 在 `report.js` 中补充模板下载 API。

---

#### 问题 11：体检报告手动生成端点前端未调用

**现象：**
- 后端 `TReportController.generate()` 提供手动报告生成
- 前端无对应 API 调用

**修复方案：** 在 `report.js` 中补充报告生成 API。

---

#### 问题 12：套餐关联检查组接口前端未覆盖

**现象：**
- 后端 `TSetmealController.getWithCheckgroups()` 返回套餐+检查组扁平列表
- 前端 `setmeal.js` 无此 API

**修复方案：** 在 `setmeal.js` 中补充套餐详情（含检查组列表）API。

---

### 🟢 P3 — 低（代码质量/清理）

#### 问题 13：已删除模块的 SQL 文件残留

**现象：**
- `doc/sql/` 目录下可能存在已删除模块（health-assessment, health-counseling, health-mood）的旧 SQL 文件
- git status 显示这些文件已被删除

**修复方案：** 确认 `doc/sql/` 下无冗余旧 SQL，统一迁移到 `sql/` 目录。

---

#### 问题 14：Git 工作区有大量未提交/已删除文件

**现象：**
- 大量 D 状态文件（已删除但未提交）
- health-Vue3 目录状态为 m（modified）
- 部分 deleted 模块（health-assessment, health-counseling, health-mood, SecurityConfig.java）

**修复方案：** 完成重构后提交或删除这些变更。

---

#### 问题 15：Swagger 测试端点暴露在生产环境

**现象：**
- `TestController` 提供 5 个测试端点 `/test/user/*`
- 在 Swagger UI 中可见

**修复方案：** 通过 `application.yml` 配置 `springdoc.api-docs.enabled` 或 `@Profile` 注解控制。

---

## 三、修复优先级与执行计划

### 第一阶段：修复编译/启动问题（P0）

| 序号 | 问题 | 预计工作量 | 风险 |
|------|------|-----------|------|
| 1 | 恢复 SecurityConfig.java | 30 分钟 | 低 — 从 git 历史恢复即可 |
| 2 | 解决 TOrdersettingController 冲突 | 30 分钟 | 低 — 删除 health-admin 副本 |

### 第二阶段：补齐缺失功能（P1）

| 序号 | 问题 | 预计工作量 | 风险 |
|------|------|-----------|------|
| 3 | 咨询师排班前端 | 2 小时 | 低 — 参考现有页面模式 |
| 4 | 用户端预约 API + 页面 | 4 小时 | 中 — 涉及新路由和页面 |
| 5 | t_report_image 完整实现 | 1 小时 | 低 — 标准 CRUD |

### 第三阶段：完善覆盖和清理（P2/P3）

| 序号 | 问题 | 预计工作量 | 风险 |
|------|------|-----------|------|
| 6-12 | 补充前端 API 覆盖 | 2 小时 | 低 |
| 7 | health-oss 清理决策 | 15 分钟 | 无 |
| 13-15 | SQL/Git/配置清理 | 30 分钟 | 低 |

**总预计工作量：约 8-10 小时**

---

## 四、详细处理步骤

### 步骤 1：恢复 Spring Security 配置

```bash
# 从 git 历史恢复 SecurityConfig.java
git show HEAD~1:health-framework/src/main/java/com/health/framework/config/SecurityConfig.java \
  > health-framework/src/main/java/com/health/framework/config/SecurityConfig.java
```

或手动创建，内容参考 git diff 中显示的原始文件（132 行），确保包含：
- `@EnableMethodSecurity(prePostEnabled = true, securedEnabled = true)`
- `SecurityFilterChain` Bean（CSRF 禁用、JWT session、匿名 URL 白名单）
- `AuthenticationManager` Bean
- `BCryptPasswordEncoder` Bean

### 步骤 2：删除 health-admin 下的 TOrdersettingController 副本

```bash
# 删除 health-admin 中的重复 Controller
rm health-admin/src/main/java/com/health/reservation/controller/TOrdersettingController.java
```

确认 health-reservation 版本的 `ITOrdersettingService` 包含所有必要方法：
- `selectTOrdersettingList()` ✅
- `selectTOrdersettingById()` ✅
- `insertTOrdersetting()` ✅
- `updateTOrdersetting()` ✅
- `editNumberByOrderDate()` ✅
- `deleteTOrdersettingByIds()` ✅
- `getOrderSettingByMonth()` ✅
- `importOrderSetting()` ✅

### 步骤 3：创建咨询师排班前端 API 模块

创建 `health-Vue3/src/api/reservation/schedule.js`：

```javascript
import request from '@/utils/request'

// 查询排班列表
export function listSchedule(query) {
  return request({ url: '/reservation/schedule/list', method: 'get', params: query })
}

// 查询排班详情
export function getSchedule(id) {
  return request({ url: `/reservation/schedule/${id}`, method: 'get' })
}

// 新增排班
export function addSchedule(data) {
  return request({ url: '/reservation/schedule', method: 'post', data })
}

// 修改排班
export function updateSchedule(data) {
  return request({ url: '/reservation/schedule', method: 'put', data })
}

// 删除排班
export function delSchedule(id) {
  return request({ url: `/reservation/schedule/${id}`, method: 'delete' })
}

// 查询某咨询师的排班
export function getCounselorSchedules(counselorId) {
  return request({ url: `/reservation/schedule/counselor/${counselorId}`, method: 'get' })
}

// 查询某日可用排班
export function getAvailableSchedules(date) {
  return request({ url: `/reservation/schedule/available/${date}`, method: 'get' })
}

// 导出排班
export function exportSchedule(query) {
  return request({ url: '/reservation/schedule/export', method: 'post', params: query, responseType: 'blob' })
}
```

### 步骤 4：创建咨询师排班管理页面

创建 `health-Vue3/src/views/reservation/schedule/index.vue`，参考 `views/reservation/checkgroup/index.vue` 的模式，包含：
- 日期范围筛选 + 咨询师下拉选择
- 排班列表表格（日期、开始时间、结束时间、总号源、剩余号源、是否可用）
- 新增/编辑对话框
- 删除按钮
- 分页

### 步骤 5：创建用户端预约 API 模块

创建 `health-Vue3/src/api/reservation/userReservation.js`：

```javascript
import request from '@/utils/request'

// 获取可预约套餐列表
export function getUserSetmeals(params) {
  return request({ url: '/reservation/user/setmeals', method: 'get', params })
}

// 获取套餐详情
export function getSetmealDetails(id) {
  return request({ url: `/reservation/user/setmeal/${id}/details`, method: 'get' })
}

// 获取可预约日期
export function getAvailableDates(params) {
  return request({ url: '/reservation/user/available-dates', method: 'get', params })
}

// 创建预约订单
export function createUserOrder(data) {
  return request({ url: '/reservation/user/order', method: 'post', data })
}

// 取消预约
export function cancelOrder(reportNo) {
  return request({ url: `/reservation/user/cancel/${reportNo}`, method: 'put' })
}
```

### 步骤 6：实现 t_report_image 完整 CRUD

**实体类** `health-reservation/.../domain/TReportImage.java`：
```java
package com.health.reservation.domain;

import com.health.common.core.domain.BaseEntity;
import org.apache.commons.lang3.builder.ToStringBuilder;
import org.apache.commons.lang3.builder.ToStringStyle;

/**
 * 体检报告图像对象 t_report_image
 * @author ruoyi
 * @date 2026-07-17
 */
public class TReportImage extends BaseEntity {
    private static final long serialVersionUID = 1L;

    private Long id;
    private Long reportId;
    private Long checkitemId;
    private String imagePath;
    private String imageDesc;
}
```

**Mapper 接口** `health-reservation/.../mapper/TReportImageMapper.java`

**Mapper XML** `health-reservation/.../mapper/reservation/TReportImageMapper.xml`

**Service** `health-reservation/.../service/ITReportImageService.java` + `impl`

**Controller** 在 `TReportController` 中增加图像上传/查询端点，或新建 `ReportImageController`

### 步骤 7：补充前端缺失的 API 函数

在现有 API 模块中补充：

**`system/post.js`** — 补充：
```javascript
export function optionselect() {
  return request({ url: '/system/post/optionselect', method: 'get' })
}
```

**`system/role.js`** — 补充：
```javascript
export function optionselect() {
  return request({ url: '/system/role/optionselect', method: 'get' })
}
```

**`tool/gen.js`** — 补充：
```javascript
export function columnList(tableId) { ... }
export function download(tableName) { ... }
export function batchGenCode(tableNames) { ... }
```

**`reservation/report.js`** — 补充：
```javascript
export function downloadReportTemplate() { ... }
export function generateReport(data) { ... }
```

**`reservation/setmeal.js`** — 补充：
```javascript
export function getSetmealWithCheckgroups(id) { ... }
export function exportSetmeal(data) { ... }
```

**`reservation/ordersetting.js`** — 补充标准 CRUD：
```javascript
export function listOrderSetting(query) { ... }
export function editOrderSetting(data) { ... }
export function delOrderSetting(ids) { ... }
export function exportOrderSetting(data) { ... }
```

**`reservation/notification.js`** — 补充：
```javascript
export function getNotificationTypes() { ... }
export function getNotification(id) { ... }
export function exportNotification(data) { ... }
```

### 步骤 8：health-oss 清理决策

```bash
# 如果需要 OSS 功能：
# 1. 创建 health-oss/pom.xml
# 2. 创建 health-oss/src/main/java/... 源码
# 3. 在根 pom.xml 添加 <module>health-oss</module>
# 4. 在 health-framework/pom.xml 添加依赖

# 如果不需要：
rm -rf health-oss/
```

### 步骤 9：SQL 文件整理

确认 `sql/` 目录下的 clean.sql、seed_data.sql、menu.sql 与数据库实际结构一致。
清理 `doc/sql/` 下的旧 SQL 文件（ai_chat.sql、ai_menu.sql 等已迁移的内容）。

### 步骤 10：Git 提交

```bash
git add -A
git commit -m "fix: 项目纠察 — 恢复安全配置、消除重复Controller、补齐缺失功能"
```

---

## 五、前后端覆盖率总结

| 模块 | 后端 Controller | 前端 API 模块 | 前端页面 | 覆盖率 |
|------|----------------|--------------|---------|--------|
| 系统管理 | 12 个 | ✅ 完整 | ✅ 完整 | 100% |
| 监控运维 | 6 个 | ✅ 完整 | ✅ 完整 | 100% |
| 代码生成 | 1 个 | ⚠️ 部分缺失 | ✅ 完整 | 90% |
| AI 对话 | 1 个 | ✅ 完整 | ✅ 完整 | 100% |
| 预约管理-检查组/项/套餐 | 5 个 | ✅ 完整 | ✅ 完整 | 100% |
| 预约管理-订单设置 | 1 个(重复) | ⚠️ 部分缺失 | ✅ 完整 | 85% |
| 预约管理-体检报告 | 2 个 | ⚠️ 部分缺失 | ✅ 完整 | 85% |
| 心理测评 | 1 个 | ✅ 完整 | ✅ 完整 | 100% |
| 情绪追踪 | 1 个 | ✅ 完整 | ✅ 完整 | 100% |
| 咨询师管理 | 1 个 | ⚠️ 部分缺失 | ⚠️ 缺排班页 | 70% |
| 咨询师排班 | 1 个 | ❌ 无 | ❌ 无 | 0% |
| 用户端预约 | 1 个 | ❌ 无 | ❌ 无 | 0% |
| 通知管理 | 1 个 | ⚠️ 部分缺失 | ✅ 完整 | 85% |
| 健康报告 | 1 个 | ✅ 完整 | ✅ 完整 | 100% |

---

## 六、数据库-实体映射完整性

| 数据库表 | 实体类 | Mapper | 状态 |
|---------|--------|--------|------|
| sys_*(20张) | ✅ | ✅ | 完整 |
| QRTZ_*(11张) | N/A | N/A | Quartz内部表，无需 |
| t_setmeal | ✅ | ✅ | 完整 |
| t_setmeal_checkgroup | ✅ | ✅ | 完整 |
| t_ordersetting | ✅ | ✅ | 完整 |
| t_checkgroup | ✅ | ✅ | 完整 |
| t_checkitem | ✅ | ✅ | 完整 |
| t_checkgroup_checkitem | ✅ | ✅ | 完整 |
| t_report | ✅ | ✅ | 完整 |
| t_report_item | ✅ | ✅ | 完整 |
| t_report_image | ❌ | ❌ | **缺失** |
| ai_conversation | ✅ | ✅ | 完整 |
| ai_message | ✅ | ✅ | 完整 |
| assessment | ✅ | ✅ | 完整 |
| assessment_question | ✅ | ✅ | 完整 |
| assessment_result | ✅ | ✅ | 完整 |
| mood_record | ✅ | ✅ | 完整 |
| counselor | ✅ | ✅ | 完整 |
| counselor_schedule | ✅ | ✅ | 完整 |
| appointment | ✅ | ✅ | 完整 |
| health_report | ✅ | ✅ | 完整 |
| sys_message | ✅ | ✅ | 完整 |

**缺失率：1/21 = 4.8%**（t_report_image）
