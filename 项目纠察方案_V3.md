# 🏥 ShanHai Health（山海健康）项目纠察方案 V3

> **版本**: 3.0（修正版，基于实际项目状态）  
> **生成时间**: 2026-07-29  
> **分支**: dev0721（当前主开发分支）  
> **技术栈**: Spring Boot 3.5.14 / Java 21 / Vue 3 + Vite + MyBatis + MySQL 8  
> **目标**: GitHub 开源发布前的核心问题修复与完整性补全  
> **适用阶段**：按天划分，每日最小可交付  

---

## 🔍 一、项目现状评估（经实地核查）

### 1.1 架构概览

```
health (Maven根项目)
├── health-admin          ← Spring Boot 入口 (端口 8090)
├ ├── health-framework      ← 安全/数据源/配置/拦截器
├ ├── health-system         ← 系统管理 (用户/角色/菜单/字典/部门)
├ ├── health-quartz         ← 定时任务 (Quartz) — ⚠️ 需启用和配⾪
├ ├── health-generator      ← 代码生成器 (Velocity)
├ ├── health-common         ← 通用工具/注解/常量/异常
├ ├── health-reservation    ← 核心业务 (预约/通知/排班/套餐等)
├ └── health-ai            ← AI 对话 (LangChain4j + DashScope) — ⚠️ 包名冲突已修复
└── health-Vue3           ← 前端 (端口 80)
```

### 1.2 已确认问题清单（修订后）

| 问题编号 | 问题描述 | 优先级 | 状态 | 备注 |
|----------|---------|--------|------|------|
| P0-01 | ScheduleConfig.java 被完全注释，Quartz调度未启用 | P0 | 待修复 | 需解除注释并配⾪ |
| P0-02 | health-quartz/pom.xml 缺 spring-boot-starter-quartz 依赖 | P0 | 待添加 | 缺少 Starter 自动配置 |
| P0-03 | application.yml 缺 spring.quartz: 配置段 | P0 | 待添加 | 即使 ScheduleConfig 启用也无法工作 |
| P1-01 | AppointmentService 缺少年/小时/分钟定时提醒逻辑 | P1 | 待实现 | 核心功能缺失 |
| P1-02 | SchedulePage.vue 仅提供表格视图，缺少日历视图 | P1 | 待完善 | 用户体验不足 |
| P2-01 | sys_post 表 AUTO_INCREMENT 可能缺失 | P2 | 待核查 | 建表语句需确认 |
| P2-02 | DashboardMapper.xml selectSetmealRanking SQL无真实销量数据 | P2 | 待修复 | 排序语义错误 |
| P3-01 | langchain4j 版本硬编码未统一管理 | P3 | 可选优化 | 多模块时可考虑 |

> **已修复问题（不在本方案执行范围内）**：
> - health-ai 包名冲突 → 已全部迁移至 `com.health.ai.*`
> - SQL 文件 syntax/INSERT IGNORE/USE health → 均已修复
> - Controller 路径前缀不统一 → 已全部加 `/reservation/` 前缀

---

## 📌 二、分阶段执行计划（共4天，28个细化步骤）

### 🗓️ 第一阶段：基石修复与环境验证（第1天，≈4小时）

**目标：确保项目可编译、可启动，Quartz基础配置正确**

| 步骤ID | 任务详情 | 执行子步骤 | 验收标准 | 负责人 | 耗时 |
|--------|----------|------------|----------|--------|------|
| 1.1 | **查看 ScheduleConfig 原内容** | 1.1.1 打开 `health-quartz/src/main/java/com/health/quartz/config/ScheduleConfig.java`<br>1.1.2 通读全部注释内容，了解原始配置<br>1.1.3 判断是否保留集群配置还是改为单机 | 明确配置修改方案 | 后端 | 0.5h |
| 1.2 | **启用 ScheduleConfig.java** | 1.2.1 移除所有行首的 `//` 注释符<br>1.2.2 将 `isClustered=true` 改为 `false`（单机部署）<br>1.2.3 将 `LocalDataSourceJobStore` 改为 `RAMJobStore`（如无石英表）或确认有 qrtz_ 表类 | 类恢复为正常 Java 类，无编译错误 | 后端 | 0.5h |
| 1.3 | **添加 Quartz 依赖** | 2.1 打开 `health-quartz/pom.xml`<br>2.2 添加依赖：<br>&lt;dependency&gt;<br> &nbsp;&lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;<br> &nbsp;&lt;artifactId&gt;spring-boot-starter-quartz&lt;/artifactId&gt;<br> &lt;/dependency&gt; | pom.xml 包含 quartz starter | 后端 | 0.5h |
| 1.4 | **验证 health-quartz 构建** | 3.1 进入 health-quartz 目录<br>3.2 执行 `mvn clean compile`<br>3.3 确认编译通过，无类找不到的错误 | quartz 模块编译成功 | 后端 | 0.5h |
| 1.5 | **添加 spring.quartz 配置** | 4.1 打开 `health-admin/src/main/resources/application.yml`<br>4.2 在文件末尾添加完整 Quartz 配置段（参照下方模板）<br>4.3 保存配置 | application.yml 含 `spring.quartz:` 配置块 | 后端 | 0.5h |
| 1.6 | **后端项目完整构建** | 5.1 进入根目录 health<br>5.2 执行 `mvn clean install -DskipTests`<br>5.3 观察构建结果，确认无报错<br>5.4 重点关注 health-quartz 和 health-admin 模块 | 全模块 BUILD SUCCESS | 后端 | 1h |
| 1.7 | **前端项目构建与启动** | 6.1 进入 health-Vue3 目录<br>6.2 执行 `npm install`（如卡住则用淘宝镜像）<br>6.3 执行 `npm run dev`<br>6.4 访问 http://localhost:80 | 前端编译成功，浏览器可访问页面 | 前端 | 1h |

> ✅ **阶段1交付物**：前后端可正常运行，Quartz 调度器基础配置完成

---

### 📝 Spring Boot Quartz 配置模板（application.yml 中添加）

```yaml
spring:
  quartz:
    scheduler:
      instance-name: RuoyiScheduler
      instance-id: AUTOMATIC
    properties:
      org:
        quartz:
          scheduler:
            instanceName: ${spring.quartz.scheduler.instance-name}
            instanceId: ${spring.quartz.scheduler.instance-id}
          jobStore:
            class: org.springframework.scheduling.quartz.JobPersistenceDelegate
            tablePrefix: QRTZ_
            isClustered: false # 单机部署设为 false
            clusterCheckinInterval: 15000
          threadPool:
            class: org.quartz.simpl.SimpleThreadPool
            threadCount: 10
            threadPriority: 5
            threadsInheritContextClassLoaderOfInitializingThread: true
```

---

### 🗓️ 第二阶段：功能补全（第2-3天，≈8小时）

**目标：补充核心业务功能和 UI 完整性**

| 步骤ID | 任务详情 | 执行子步骤 | 验收标准 | 负责人 | 耗时 |
|--------|----------|------------|----------|--------|------|
| 2.1 | **核查 sys_post 表结构** | 1.1 登录 MySQL<br>1.2 执行 `USE health; DESCRIBE sys_post;`<br>1.3 检查 id 列是否有 AUTO_INCREMENT 属性 | 得到自增状态结论 | 运维 | 0.5h |
| 2.2 | **补全自增（如缺失）** | 2.1 如缺失自增，执行：<br>`ALTER TABLE sys_post MODIFY id BIGINT(20) NOT NULL AUTO_INCREMENT PRIMARY KEY;`<br>2.2 再次 DESCRIBE 验证 | ALTER 执行成功，id 列为 AUTO_INCREMENT | 运维 | 0.5h |
| 2.3 | **DashboardMapper SQL修复** | 3.1 打开 `health-reservation/mapper/ReservationMapper.xml`<br>3.2 找到 selectSetmealRanking 方法<br>3.3 替换 SQL 为基于 t_report LEFT JOIN 的销量统计查询 | SQL 按实际销售数量排序，非 ID 顺序 | 后端 | 1h |
| 2.4 | **添加 appointment reminder_sent 字段** | 4.1 执行：<br>`ALTER TABLE appointment ADD COLUMN reminder_sent TINYINT(1) DEFAULT 0;`<br>4.2 验证字段存在 | 表结构变更成功 | 运维 | 0.5h |
| 2.5 | **实现 ReminderJob（Quartz 任务）** | 5.1 创建 `ReminderJob.java` 继承 AbstractQuartzJob<br>5.2 注入 IAppointmentService 和 INotificationService<br>5.3 编写查询逻辑：查 reminder_sent=0、appointment_time 在未来30min~24h内、status=1的记录<br>5.4 调用 notificationService 创建通知，更新 reminder_sent=1 | Job 编译并通过测试 | 后端 | 2h |
| 2.6 | **修改 confirmAppointment 设置提醒时间** | 6.1 在 AppointmentServiceImpl.confirmAppointment() 中：<br>&nbsp;&nbsp;appointment.setReminderTime(DateUtils.addMinutes(new Date(), 30));<br>&nbsp;&nbsp;appointment.update();<br>6.2 保存设置后的记录 | 确认预约时提醒时间被设置 | 后端 | 1h |
| 2.7 | **SchedulePage 添加 el-calendar 日历视图** | 7.1 在 template 中添加 `<el-calendar>` 组件<br>7.2 绑定 cell-slot 插槽显示可用时段数<br>7.3 编写方法获取某天排班数据，按数量着色（>0 绿色，0 灰色）<br>7.4 点击日期联动表格筛选 | 日历与表格数据同步，颜色区分明显 | 前端 | 2h |

> ✅ **阶段2交付物**：系统完整性提升，排行榜数据正确，预约有提醒逻辑，排班有日历视图

---

### 🗓️ 第三阶段：SecurityConfig 审计（第4天，≈2小时）

**目标：清理冗余，完善认证配置**

| 步骤ID | 任务详情 | 执行子步骤 | 验收标准 | 负责人 | 耗时 |
|--------|----------|------------|----------|--------|------|
| 3.1 | **审查 PermitAllUrlProperties** | 1.1 打开 PermitAllUrlProperties.java<br>1.2 该类目前收集 @Anonymous 注解路径，但项目中无 @Anonymous 注解<br>1.3 可考虑删除该类（如不用）或保留作为未来扩展 | 确定去留，若删除则在 SecurityConfig 中引用移除 | 后端 | 0.5h |
| 3.2 | **CSRF 策略确认** | 2.1 查看 SecurityConfig 中 `.csrf(csrf -> csrf.disable())`<br>2.2 确认已有注释说明禁用原因（前后端分离 + token 认证） | 有 CSRF disable 及理由注释 | 后端 | 0.5h |
| 3.3 | **AuthenticationManager 验证** | 3.1 确认 @Bean public AuthenticationManager authenticationManager() 存在且正确 | Bean 定义无误 | 后端 | 0.5h |
| 3.4 | **登录流程测试** | 4.1 启动后端，访问 http://localhost:8090/swagger-ui.html<br>4.2 使用默认账号 admin/admin123 登录<br>4.3 测试获取 Token、受保护接口访问、登出 | 登录流程完整可用 | 测试 | 0.5h |

> ✅ **阶段4交付物**：SecurityConfig 精简完整，认证流程通过测试

---

### 🗓️ 第四阶段：发布准备（第4天后半段，≈2小时）

**目标：为 GitHub 发布做最终准备**

| 步骤ID | 任务详情 | 执行子步骤 | 验收标准 | 负责人 | 耗时 |
|--------|----------|------------|----------|--------|------|
| 4.1 | **完善 README.md** | 1.1 添加项目简介（功能概述、技术栈）<br>1.2 添加快速启动指南（前端 npm run dev + 后端 mvn spring-boot:run）<br>1.3 添加数据库初始化说明（SQL 执行顺序）<br>1.4 添加默认账号信息 | README 可指导新用户快速运行 | 后端 | 1h |
| 4.2 | **.gitignore 检查** | 2.1 检查根目录 .gitignore<br>2.2 确保包含：/target/、/node_modules/.idea/、*.log、*.env<br>2.3 如有缺失则补充 | 无敏感/本地文件纳入版本控制 | 前端/后端 | 0.5h |
| 4.3 | **提交变更并打 Tag** | 3.1 git add .<br>3.2 git commit -m "feat: complete core fixes for v1.0.0"<br>3.3 git tag -a v1.0.0 -m "Initial Release"<br>3.4 git push --tags | Tag 创建并推送到远程 | 运维 | 0.5h |

> ✅ **阶段5交付物**：可发布的版本文档完备，Tag 就绪

---

## 📊 三、阶段依赖关系

```
第1天: 基石修复 (Compile + Start + Quartz config)
        │
        ▼
第2-3天: 功能补全 (Dashboard SQL + Reminder logic + Calendar view)
        │
        ▼
第4天: Security审计 + 发布准备 (README + .gitignore + Tag)
```

---

## ⚠️ 四、风险预案

| 风险点 | 触发条件 | 影响等级 | 应对措施 |
|--------|----------|----------|---------|
| ScheduleConfig 启用后 Quartz 初始化失败 | 启动日志出现 ClassCastException 或 NoDataSource | 高 | 1. 确认 application.yml 中 dataSource 配置正确；2. 检查有否 qrtz_ 表（如无则改用 RAMJobStore）；3. 启用内存模式测试 |
| reminder_sent 字段已存在 | ALTER TABLE 报错 column exists | 中 | 执行 `SHOW COLUMNS FROM appointment LIKE 'reminder_sent';` 确认不存在再添加 |
| el-calendar 组件数据不同步 | 日历显示与表格不一致 | 中 | 1. 统一使用同一个后端 `/reservation/schedule/list` 接口；2. 日历和表格使用相同日期查询参数 |
| npm install 卡住 | Node 模块下载缓慢 | 中 | 1. 切换淘宝镜像：`npm config set registry https://registry.npmmirror.com`；2. 删除 node_modules 和 package-lock.json 后重装 |

---

## 📝 五、检查清单（逐项打勾）

### [ ] 第一阶段完成
- [ ] 1.1 ScheduleConfig 原内容审查完毕
- [ ] 1.2 ScheduleConfig 启用并调整集群配置为单机
- [ ] 1.3 health-quartz/pom.xml 添加了 spring-boot-starter-quartz
- [ ] 1.4 health-quartz 模块编译通过
- [ ] 1.5 application.yml 添加了 spring.quartz: 配置
- [ ] 1.6 mvn clean install 全模块构建成功
- [ ] 1.7 前端构建启动成功

### [ ] 第二阶段完成
- [ ] 2.1 sys_post 表 AUTO_INCREMENT 核查
- [ ] 2.2 补全自增（如需）
- [ ] 2.3 DashboardMapper ranking SQL 修复
- [ ] 2.4 appointment 表添加 reminder_sent 字段
- [ ] 2.5 ReminderJob 创建并实现提醒逻辑
- [ ] 2.6 confirmAppointment 设置提醒时间
- [ ] 2.7 SchedulePage 添加 el-calendar 日历视图

### [ ] 第三阶段完成
- [ ] 3.1 PermitAllUrlProperties 审查处理
- [ ] 3.2 CSRF 配置确认有注释
- [ ] 3.3 AuthenticationManager Bean 验证
- [ ] 3.4 完整登录流程测试通过

### [ ] 第四阶段完成
- [ ] 4.1 README.md 完善（简介、启动指南、数据库说明）
- [ ] 4.2 .gitignore 检查完整
- [ ] 4.3 git commit 并提交 v1.0.0 tag

---

**方案制定人：** Codex（基于项目实际状态修订）  
**最后更新时间：** 2026-07-29  
**文件保存路径：** `D:\study1\health\项目纠察方案_V3.md`

---

> 💡 **使用说明**：此为针对当前项目状态的精简实用方案，优先保证核心功能可用、项目可编译启动、README 能指导快速入门。非阻塞性、美观性问题可在后续迭代中逐步完善。
