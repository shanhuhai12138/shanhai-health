# 🏥 ShanHai Health（山海健康）项目纠察方案 V2

> **版本**: 2.1（分阶段细化版）  
> **生成时间**: 2026-07-29  
> **分支**: dev0721（当前主开发分支）  
> **技术栈**: Spring Boot 3.5.14 / Java 21 / Vue 3 + Vite + MyBatis + MySQL 8 + Redis + LangChain4j  
> **目标**: GitHub 开源发布前的全面质量检查与功能补全  
> **适用阶段**: 按天划分，每日最小可交付  

---

## 📋 一、项目现状评估

### 1.1 架构概览

health (Maven根项目)
├── health-admin          ← Spring Boot 入口 (端口 8090)
├── health-framework      ← 安全/数据源/配置/拦截器
├── health-system         ← 系统管理 (用户/角色/菜单/字典/部门)
├── health-quartz         ← 定时任务 (Quartz)  ← ScheduleConfig 被注释
├ ├── health-generator    ← 代码生成器 (Velocity)
├ ├── health-common       ← 通用工具/注解/常量/异常
├ ├── health-reservation  ← 核心业务 (预约/通知/排班)  ← 缺定时提醒
├ ├── health-ai           ← AI 对话 (LangChain4j + DashScope)
└── health-Vue3         ← 前端 (端口 80)  ← SchedulePage 只有表格无日历

### 1.2 已确认问题清单

| 问题编号 | 问题描述 | 优先级 | 状态 |
|----------|----------|--------|------|
| P0-01 | SecurityConfig.java 冗余白名单配置 | P0 | 待核实 |
| P0-02 | ScheduleConfig.java 被完全注释，Quartz调度未启用 | P0 | 待修复 |
| P0-03 | AppointmentService 缺少年/小时/分钟定时提醒逻辑 | P1 | 待实现 |
| P0-04 | SchedulePage.vue 仅提供表格视图，缺少日历视图 | P1 | 待完善 |
| P0-05 | sys_post 表 AUTO_INCREMENT 可能缺失 | P2 | 待核查 |

---

## 📌 二、分阶段执行计划（共5天，31个细化步骤）

### 🗓️ 第一阶段：基础修复与环境验证（第1天，≈4小时）

**目标：确保项目可编译、可启动，核心配置正常**

| 步骤ID | 任务详情 | 执行子步骤 | 验收标准 | 负责人 | 耗时 |
|--------|----------|------------|----------|--------|------|
| 1.1 | **启用 ScheduleConfig.java** | 1.1.1 打开 `health-quartz/src/main/java/com/health/quartz/config/ScheduleConfig.java`<br>1.1.2 移除所有行首的 `//` 注释符<br>1.1.3 检查类内所有Bean定义是否完整<br>1.1.4 保存文件 | 类恢复为正常Java类，无编译错误 | 后端 | 0.5h |
| 1.2 | **验证 Quartz 配置** | 2.1 打开 `health-admin/src/main/resources/application.yml`<br>2.2 查找 `spring.quartz:` 配置段<br>2.3 确认 `store-db: true`<br>2.4 确认 `scheduler: instanceName: RuoyiScheduler`<br>2.5 启动项目观察日志 | `application.yml` 中Quartz配置生效，启动日志无Quartz报错 | 后端 | 0.5h |
| 1.3 | **数据库表结构核查** | 3.1 登录MySQL<br>3.2 执行 `USE health_db;`<br>3.3 执行 `DESCRIBE sys_post;`<br>3.4 检查 `id` 列是否有 `AUTO_INCREMENT` | 得到 `sys_post` 表结构，确认自增状态 | 运维 | 0.5h |
| 1.4 | **补全自增（如缺失）** | 4.1 如缺失自增，执行：<br>`ALTER TABLE sys_post MODIFY id BIGINT(20) NOT NULL AUTO_INCREMENT PRIMARY KEY;`<br>4.2 执行 `DESCRIBE sys_post;` 再次确认 | 执行成功，`id` 列为 `AUTO_INCREMENT` | 运维 | 0.5h |
| 1.5 | **后端项目构建与启动** | 5.1 进入 `health-admin` 目录<br>5.2 执行 `mvn clean install -DskipTests`<br>5.3 构建成功后执行 `mvn spring-boot:run`<br>5.4 访问 `http://localhost:8090/` | 构建无错误，端口8090可访问，返回200 | 后端 | 1h |
| 1.6 | **前端项目构建与启动** | 6.1 进入 `health-Vue3` 目录<br>6.2 执行 `npm install`（如缺少依赖）<br>6.3 执行 `npm run dev`<br>6.4 访问 `http://localhost:80` | 前端编译成功，浏览器可访问页面，Network 无404 | 前端 | 1h |

> ✅ **阶段1交付物**：前后端可正常运行，P0基础问题可验证

---

### 🗓️ 第二阶段：定时任务与提醒机制（第2-3天，≈12小时）

**目标：实现预约定时提醒功能**

| 步骤ID | 任务详情 | 执行子步骤 | 验收标准 | 负责人 | 耗时 |
|--------|----------|------------|----------|--------|------|
| 2.1 | **创建 ReminderJob 类** | 1.1 创建 `health-quartz/src/main/java/com/health/quartz/task/ReminderJob.java`<br>1.2 继承 `AbstractQuartzJob`<br>1.3 重写 `doJob` 方法<br>1.4 添加 `@Service("reminderJob")` | 类编译通过，无报错 | 后端 | 1h |
| 2.2 | **实现提醒查询逻辑** | 2.1 在 ReminderJob 中注入 `IAppointmentService`<br>2.2 编写查询逻辑：查找状态=`1`（已确认）、`appointment_time` 在 `[当前时间+30min, 当前时间+24h]` 之间、`reminder_sent=0` 的预约<br>2.3 返回结果列表 | 可正确查出待提醒数据 | 后端 | 2h |
| 2.3 | **集成通知服务** | 3.1 注入 `INotificationService`<br>3.2 对每个待提醒记录调用 `notificationService.createNotification(userId, "appointment", "提醒", "您的预约即将开始，请准时参加", appointmentId, "appointment")`<br>3.3 更新 `reminder_sent=1` | 通知记录写入数据库 | 后端 | 1h |
| 2.4 | **配置 Cron 表达式** | 4.1 在 `SysJobServiceImpl` 中添加提醒任务记录<br>4.2 Cron 设为 `0 0/30 * * * ?`（每30分钟执行一次）<br>4.3 targetObject 设为 `reminderJob`<br>4.4 Cron表达式验证通过 | Cron 表达式正确，任务可配置到数据库 | 后端 | 0.5h |
| 2.5 | **添加提醒标记字段** | 5.1 执行数据库迁移：<br>`ALTER TABLE appointment ADD COLUMN reminder_sent TINYINT(1) DEFAULT 0;`<br>5.2 验证字段存在 | 表结构变更成功，字段存在 | 运维 | 1h |
| 2.6 | **修改 AppointmentServiceImpl - confirm** | 6.1 在 `confirmAppointment` 方法中设置触发时间<br>`appointment.setReminderTime(DateUtils.addMinutes(new Date(), 30));` // 可配置<br>6.2 保存更新 | 确认预约时提醒时间被设置 | 后端 | 1h |
| 2.7 | **修改 AppointmentServiceImpl - cancel** | 7.1 在 `cancelAppointment` 中清理相关提醒<br>7.2 可调用通知服务删除未发送的提醒 | 取消后提醒被清除 | 后端 | 1h |
| 2.8 | **前端提醒列表页面** | 8.1 创建 `health-Vue3/src/views/notification/list.vue`<br>8.2 添加 API 接口 `src/api/notification.js`<br>8.3 使用 el-table 展示提醒记录（含预约时间、状态、发送时间）<br>8.4 添加分页和筛选 | 前端可查看提醒记录列表 | 前端 | 2h |
| 2.9 | **测试验证** | 9.1 创建一条预约（时间在未来1小时内）<br>9.2 等待或手动触发提醒任务<br>9.3 检查数据库是否有通知记录<br>9.4 前端页面刷新可见<br>9.5 测试重复提醒是否被阻止（reminder_sent=1） | 提醒按时生成，无重复记录，前端可见 | 测试 | 2.5h |

> ✅ **阶段2交付物**：定时提醒功能完整，前端可管理提醒记录

---

### 🗓️ 第三阶段：前端日历视图完善（第4天，≈8小时）

**目标：为 SchedulePage 添加 el-calendar 日历视图**

| 步骤ID | 任务详情 | 执行子步骤 | 验收标准 | 负责人 | 耗时 |
|--------|----------|------------|----------|--------|------|
| 3.1 | **分析排班数据逻辑** | 1.1 查看现有 `getList()` 如何获取排班数据<br>1.2 分析 `slotDuration`、`totalSlots`、`availableSlots` 的计算方式<br>1.3 确认后端 `/reservation/schedule/list` 返回结构 | 清晰理解数据流向，准备日历数据格式 | 前端 | 1h |
| 3.2 | **添加 el-calendar 组件** | 2.1 在 template 中新增 `<el-calendar>`<br>2.2 设置 `v-model="calendarDate"`<br>2.3 配置 `type: "date"`<br>2.4 添加范围选择器与今日按钮 | 日历组件正常渲染，可选择日期 | 前端 | 1h |
| 3.3 | **日历数据绑定** | 3.1 编写方法获取某天排班数据：调用 `/reservation/schedule/list?scheduleDate=YYYY-MM-DD`<br>3.2 使用 `cell-slot` 自定义节点显示可用时段数<br>3.3 可用时段用不同颜色标记（如 >0 绿色，0 灰色） | 日历单元格显示可用时段数，颜色区分 | 前端 | 2h |
| 3.4 | **日历点击跳转** | 4.1 添加 `@click` 事件处理<br>4.2 点击日期时设置 `queryParams.scheduleDate = 选中日期`<br>4.3 调用 `handleQuery()` 刷新表格 | 点击日历日期，表格自动筛选该日期数据 | 前端 | 1h |
| 3.5 | **新增快捷入口** | 5.1 在日历旁添加按钮"今日排班"<br>5.2 按钮点击：设置日期为今天，打开新增对话框 | 按钮存在且功能正常 | 前端 | 0.5h |
| 3.6 | **样式适配** | 6.1 检查移动端日历宽度<br>6.2 确保表格在窄屏下可滚动<br>6.3 整体布局保持一致性 | 桌面/手机均正常显示 | 前端 | 1.5h |
| 3.7 | **功能测试** | 7.1 选择不同日期范围<br>7.2 点击不同日期验证筛选<br>7.3 新增排班后刷新日历验证数据同步 | 日历与表格数据同步，筛选准确 | 测试 | 1h |

> ✅ **阶段3交付物**：表格+日历双视图排班管理，数据实时同步

---

### 🗓️ 第四阶段：SecurityConfig 完整审计（第5天，≈4小时）

**目标：彻底清理冗余，完善认证策略**

| 步骤ID | 任务详情 | 执行子步骤 | 验收标准 | 负责人 | 耗时 |
|--------|----------|------------|----------|--------|------|
| 4.1 | **审计 @Anonymous 白名单** | 1.1 打开 `health-framework/src/main/java/com/health/framework/config/SecurityConfig.java`<br>1.2 搜索所有 `@Anonymous` 注解<br>1.3 检查是否有路径重复配置（如 `/login` 出现多次）<br>1.4 删除重复项，保留唯一配置 | 每个 URL 路径仅有一个 `@Anonymous` | 后端 | 1h |
| 4.2 | **审查 CSRF 策略** | 2.1 查找 `.csrf()` 配置<br>2.2 确认是否调用 `.disable()`<br>2.3 在类顶部添加注释说明禁用原因（前后端分离）<br>2.4 确认无遗漏的 CSRF 保护 | `csrf().disable()` 明确存在，有注释说明 | 后端 | 1h |
| 4.3 | **验证 BCryptPasswordEncoder** | 3.1 检查 `@Bean public BCryptPasswordEncoder passwordEncoder()` 是否存在<br>3.2 检查是否被正确注入到需要密码编码的地方 | Bean 存在且被使用 | 后端 | 0.5h |
| 4.4 | **AuthenticationManager 检查** | 4.1 确认 `@Bean public AuthenticationManager authenticationManager()` 存在<br>4.2 检查是否使用了正确的 UserDetailsService | 认证管理器 Bean 正确定义 | 后端 | 0.5h |
| 4.5 | **完整登录流程测试** | 5.1 测试正常登录<br>5.2 测试错误密码<br>5.3 测试未授权访问受保护接口<br>5.4 测试登出功能<br>5.5 验证 Token 刷新机制 | 所有认证流程正常，权限校验生效 | 测试 | 1h |

> ✅ **阶段4交付物**：SecurityConfig 精简完整，认证测试通过

---

### 🗓️ 第五阶段：发布前全面检查与文档完善（第6天，≈6小时）

**目标：为 GitHub 开源发布做最终准备**

| 步骤ID | 任务详情 | 执行子步骤 | 验收标准 | 负责人 | 耗时 |
|--------|----------|------------|----------|--------|------|
| 5.1 | **更新项目纠察方案文档** | 1.1 将本 V2.1 方案保存为 `项目纠察方案.md`<br>1.2 添加每个阶段的完成标记（✓）<br>1.3 添加实际执行时间备注 | 文档完整，步骤可追溯 | 后端 | 1h |
| 5.2 | **完善 README.md** | 2.1 添加项目简介<br>2.2 添加技术栈清单<br>2.3 添加快速启动指南（前后端启动命令）<br>2.4 添加数据库初始化说明 | README 可指导新用户快速运行 | 后端 | 1h |
| 5.3 | **代码规范检查** | 3.1 在 `health-admin` 执行 `mvn checkstyle:check`<br>3.2 执行 `mvn pmd:check`<br>3.3 修复出现的警告 | 检查通过，无严重警告 | 后端 | 1h |
| 5.4 | **单元测试覆盖检查** | 4.1 查看 `health-reservation` 模块测试目录<br>4.2 确认 AppointmentService 有单元测试<br>4.3 确认 NotificationService 有测试<br>4.4 如有缺失，补最少覆盖关键逻辑的测试 | 关键 Service 有基础测试用例 | 测试 | 1h |
| 5.5 | **数据库迁移脚本** | 5.1 在 `sql` 目录创建 `schema.sql`（建表语句）<br>5.2 创建 `data.sql`（初始化数据：系统用户、角色、菜单）<br>5.3 验证脚本可执行 | 脚本可重建完整数据库环境 | 运维 | 1h |
| 5.6 | **.gitignore 检查** | 6.1 检查 `.gitignore` 是否包含：<br>   - `/.idea/`<br>   - `/.vs/`<br>   - `/node_modules/`<br>   - `/target/`<br>   - `*.log`<br>   - `.env.local`<br>6.2 补充缺失项 | 无敏感/本地文件被纳入版本控制 | 前端/后端 | 0.5h |
| 5.7 | **准备发布标签** | 7.1 执行 `git add .`<br>7.2 执行 `git commit -m "feat: prepare v1.0.0 release"`<br>7.3 执行 `git tag -a v1.0.0 -m "Initial Release"`<br>7.4 执行 `git push --tags` | Tag 创建成功，可推送到远程 | 运维 | 0.5h |

> ✅ **阶段5交付物**：可发布的版本，文档完备，Tag 就绪

---

## 📊 三、阶段依赖与并行关系

┌─────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│  第1天：基础修复 │────▶│  第2-3天：提醒   │────▶│  第4天：日历视图  │
│  (4小时)        │     │  (12小时)        │     │  (8小时)         │
└─────────────────┘     └──────────────────┘     └──────────────────┘
       │                         ▲                    │
       │                         │                    │
       ▼                         │                    │
┌─────────────────┐            │                    │
│  第5天：Security│◀───────────┘                    │
│  审计（4小时）  │                                │
└─────────────────┘                                │
                                                   │
                                                   ▼
                                         ┌──────────────────┐
                                         │ 第6天：发布检查   │
                                         │  (6小时)         │
                                         └──────────────────┘

> **并行建议**：第2-3天（提醒机制）与第4天（日历视图）可并行开发，分别由后端和前端负责，每日同步进度。

---

## ⚠️ 四、风险预案

| 风险点 | 触发条件 | 影响等级 | 应对措施 |
|--------|----------|----------|----------|
| ScheduleConfig 启用后 Quartz 初始化失败 | 启动日志出现 `ClassCastException` 或 `NoDataSource` | 高 | 1. 确认 `application.yml` 中 DataSource 配置正确<br>2. 检查 `health-quartz` 依赖是否已引入 `pom.xml`<br>3. 如为单机部署，可暂时启用内存模式（`LocalDataSourceJobStore` → `RAMJobStore`） |
| 提醒任务 Cron 触发过于频繁 | 数据库通知表写入量激增 | 中 | 1. 调整为每 15-30 分钟扫描一次<br>2. 增加扫描时间范围限制（如只查未来2小时内的提醒）<br>3. 添加任务执行次数限制 |
| 日历与表格数据不一致 | 前端刷新后日历未更新显示 | 中 | 1. 统一使用后端 `/reservation/schedule/list` 接口数据<br>2. 日历组件使用相同的日期筛选参数<br>3. 增加数据同步的调试日志输出 |
| SecurityConfig 修改后认证异常 | 登录返回 401/403 | 高 | 1. 修改前备份原文件<br>2. 每次修改后测试登录流程<br>3. 如出现问题，Git revert 到上一版本 |
| 前端依赖安装失败 | `npm install` 长时间卡住 | 中 | 1. 切换淘宝镜像：`npm config set registry https://registry.npmmirror.com`<br>2. 删除 `node_modules` 和 `package-lock.json` 后重装 |

---

## 📝 五、检查清单（逐项打勾）

### [ ] 第一阶段完成
- [ ] 1.1 ScheduleConfig 启用编译通过
- [ ] 1.2 Quartz 配置验证正常
- [ ] 1.3 sys_post 表自增核查
- [ ] 1.4 自增补全（如需要）
- [ ] 1.5 后端构建启动成功
- [ ] 1.6 前端构建启动成功

### [ ] 第二阶段完成
- [ ] 2.1 ReminderJob 类创建
- [ ] 2.2 提醒查询逻辑实现
- [ ] 2.3 通知服务集成
- [ ] 2.4 Cron 配置正确
- [ ] 2.5 reminder_sent 字段添加
- [ ] 2.6 confirmAppointment 设置提醒时间
- [ ] 2.7 cancelAppointment 清理提醒
- [ ] 2.8 前端提醒列表页面
- [ ] 2.9 测试验证通过

### [ ] 第三阶段完成
- [ ] 3.1 排班数据逻辑分析
- [ ] 3.2 el-calendar 组件添加
- [ ] 3.3 日历数据绑定显示
- [ ] 3.4 日历点击跳转筛选
- [ ] 3.5 今日快捷排班按钮
- [ ] 3.6 样式响应式适配
- [ ] 3.7 功能测试通过

### [ ] 第四阶段完成
- [ ] 4.1 @Anonymous 白名单去重
- [ ] 4.2 CSRF 策略确认禁用
- [ ] 4.3 BCryptPasswordEncoder 验证
- [ ] 4.4 AuthenticationManager 检查
- [ ] 4.5 完整登录测试

### [ ] 第五阶段完成
- [ ] 5.1 纠察方案文档更新
- [ ] 5.2 README.md 完善
- [ ] 5.3 代码规范检查通过
- [ ] 5.4 单元测试覆盖
- [ ] 5.5 数据库迁移脚本
- [ ] 5.6 .gitignore 检查
- [ ] 5.7 发布标签 v1.0.0 创建

---

**方案制定人：** Codex  
**最后更新时间：** 2026-07-29 23:00  
**文件保存路径：** `D:\study1\health\项目纠察方案.md`

---

> 💡 **使用建议**：每日开始前对照检查清单，每完成一步打勾并记录实际耗时。如有延期，在方案中备注原因，便于版本迭代复盘。