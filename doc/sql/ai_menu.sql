-- ----------------------------
-- 1. 先父菜单：龙之AI小助手（目录级）
-- ----------------------------
INSERT INTO sys_menu (menu_name, parent_id, order_num, path, component, is_frame, menu_type, visible, status, perms, icon, create_by, create_time, remark)
VALUES ('龙之AI小助手', 0, 9, 'ai-chat', 'layout', 1, 'M', '0', '0', '', 'guide', 'admin', NOW(), 'AI智能助手菜单');

-- 注意：上面的 parent_id=0 表示顶级菜单。
-- 如果你希望放在某个现有菜单下（比如"系统监控"下），请先查 sys_menu 找到对应 parentId，然后替换上面的 parent_id。

-- 2. 子菜单：AI对话页面（按钮级，实际页面通过路由访问）
-- 假设上面插入的 sys_menu 自增ID为 100（请根据实际情况调整）
-- 更推荐的方式：在若依后台"菜单管理"中手动添加，步骤如下：

-- ============================================================
-- 推荐方式：通过若依后台手动添加菜单
-- ============================================================
-- 步骤：
--   1. 登录若依后台 → 系统管理 → 菜单管理
--   2. 点击"新增"，填写：
--      - 菜单名称：龙之AI小助手
--      - 父级菜单：无（顶级菜单）
--      - 菜单类型：目录
--      - 路由地址：ai-chat
--      - 组件路径：ai/chat
--      - 排序号：9
--      - 菜单标识：ai-chat
--      - 权限标识：ai:chat:view
--      - 图标：guide
--   3. 再次点击"新增"，填写：
--      - 菜单名称：AI对话
--      - 父级菜单：龙之AI小助手
--      - 菜单类型：菜单
--      - 路由地址：chat
--      - 组件路径：ai/chat
--      - 权限标识：ai:chat:chat
--      - 是否外链：否
--   4. 保存后，在"角色管理"中给对应角色分配该菜单权限
--   5. 重新登录即可看到新菜单

-- ============================================================
-- SQL建表（如果还未执行）
-- ============================================================
-- 请确保已执行 doc/sql/ai_chat.sql 中的建表语句：
--   CREATE TABLE ai_conversation (...)
--   CREATE TABLE ai_message (...)
