<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://www.w3.org/2001/XMLSchema-instance http://www.w3.org/2001/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>health</artifactId>
        <groupId>com.health</groupId>
        <version>3.9.2</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>health-quartz</artifactId>

    <description>
        quartz定时任务
    </description>

    <dependencies>

        <!-- Spring Boot Quartz Starter -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-quartz</artifactId>
        </dependency>

        <!-- 定时任务核心库 -->
        <dependency>
            <groupId>org.quartz-scheduler</groupId>
            <artifactId>quartz</artifactId>
        </dependency>

        <!-- 通用工具-->
        <dependency>
            <groupId>com.health</groupId>
            <artifactId>health-common</artifactId>
        </dependency>

        <!-- 预约模块（用于ReminderJob访问Appointment和INotificationService） -->
        <dependency>
            <groupId>com.health</groupId>
            <artifactId>health-reservation</artifactId>
        </dependency>

    </dependencies>

</project>
